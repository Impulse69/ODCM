require('./Config/db').query("SELECT s.id, s.plate_number, s.sms_status, s.sms_sent_at, s.last_sms_type, COALESCE(ic.name, co.company_name) AS customer_name FROM subscriptions s LEFT JOIN individual_customers ic ON ic.id = s.individual_customer_id LEFT JOIN companies co ON co.id = s.company_id WHERE s.sms_sent_at IS NOT NULL ORDER BY s.sms_sent_at DESC LIMIT 20").then(res => console.log('OK')).catch(err => console.error(err.message)).finally(() => process.exit(0))
